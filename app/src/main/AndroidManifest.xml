<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ar.docscanner">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="com.MainApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.FolderLockerActivity"></activity>
        <activity android:name=".activities.DonationsActivity" />
        <activity android:name=".activities.FileManagerActivity" />
        <activity android:name=".activities.FilterImageActivity" />
        <activity android:name=".activities.DocumentManager" />
        <activity android:name=".activities.ExtractText" />
        <activity android:name=".activities.DashboardActivity" />
        <activity
            android:name="com.SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <meta-data -->
        <!-- android:name="com.google.android.gms.ads.APPLICATION_ID" -->
        <!-- android:value="@string/admod_app_id"/> -->
        <!-- HUNGDH -->
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:noHistory="true" />
        <activity
            android:name="com.facebook.ads.AudienceNetworkActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:noHistory="true" />

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activities.MyPDFActivity"
            android:label="@string/my_pdf"
            android:theme="@style/AppTheme" /> <!-- DOCUMENT SCANNER -->
        <activity
            android:name=".activities.SimpleDocumentScannerActivity"
            android:label="@string/from_gallery"
            android:theme="@style/AppTheme" /> <!-- IMAGE TO PDF -->
        <activity
            android:name="com.gun0912.tedpicker.ImagePickerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme_picker" /> <!-- OPEN NOTE SCANNER -->
        <!-- PHOTO EDITOR -->
        <activity android:name=".process.view.ProcessImageActivity" />
        <activity android:name="com.joshuabutton.queenscanner.sign.view.SignActivity" />
        <activity android:name="com.joshuabutton.queenscanner.process.view.CombineBitMapActivity" />
        <activity android:name=".document.DocumentActivity" />
        <activity android:name="com.joshuabutton.queenscanner.handle.HandleActivity" />
        <activity android:name=".listdoc.DocsActivity" />
    </application>

</manifest>